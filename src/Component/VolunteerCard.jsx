import Button from "react-bootstrap/Button";
import Card from "react-bootstrap/Card";

export default function VolunteerOppsCard({
  title
  }) {
  

  return (
    <div className="container text-center my-5">
      <h2 className="fs-1 mySubheading">{title}</h2>

      
    </div>
  );
}
